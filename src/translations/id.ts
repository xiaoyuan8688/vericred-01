import { Translation } from '../types';
import en from './en';

const id: Translation = {
  ...en,
  // Reserved for Indonesian translation
  splashTitle: "HENGXIN VERICRED [id]",
  brandName: "VeriCred | 衡信 [id]",
};

export default id;
